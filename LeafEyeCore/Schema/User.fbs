include "Profile.fbs";
/// objectbox:relation(name=profile,to=Profile)
table User {
    /// objectbox: id
    id: ulong;
    /// objectbox: index=hash64
    username: string;
    password: string;
    is_admin: bool;
}