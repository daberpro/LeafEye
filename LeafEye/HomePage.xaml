<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LeafEye.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LeafEye"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="40" RowDefinitions="Auto, *, Auto">

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel Spacing="4">
                <TextBlock Text="Upload gambar daun untuk mendeteksi penyakit." Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
                <Button Content="Pilih Folder Output" Style="{StaticResource ShadcnOutlineButtonStyle}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,24,0">
                <Rectangle Stroke="{ThemeResource SurfaceStrokeColorDefaultBrush}" 
                           StrokeThickness="2" 
                           StrokeDashArray="4 4" 
                           RadiusX="8" RadiusY="8" 
                           Fill="{ThemeResource LayerFillColorAltBrush}"/>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16">
                    <Border Background="{ThemeResource LayerFillColorDefaultBrush}" Width="64" Height="64" CornerRadius="32" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1">
                        <FontIcon Glyph="&#xE8B0;" FontSize="24" Foreground="{StaticResource BrandPrimaryBrush}"/>
                    </Border>

                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Drag &amp; Drop gambar di sini" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
                        <TextBlock Text="atau klik tombol di bawah" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <Button Content="Pilih File" Style="{StaticResource ShadcnOutlineButtonStyle}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <Border Grid.Column="1" Style="{StaticResource ShadcnCardStyle}" Margin="0">
                <Grid RowDefinitions="Auto, *">
                    <TextBlock Text="Antrian File" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,12"/>

                    <ListView Grid.Row="1" SelectionMode="None">
                        <Grid ColumnDefinitions="*, Auto" Padding="0,8">
                            <StackPanel Spacing="2">
                                <TextBlock Text="durian_leaf_001.jpg" Style="{StaticResource BodyTextBlockStyle}"/>
                                <TextBlock Text="2.4 MB" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                            <FontIcon Grid.Column="1" Glyph="&#xE73E;" FontSize="16" Foreground="{StaticResource BrandPrimaryBrush}"/>
                        </Grid>
                    </ListView>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Margin="0,24,0,0" Padding="24" CornerRadius="8" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Grid ColumnDefinitions="*, Auto">
                <StackPanel Spacing="4" VerticalAlignment="Center">
                    <TextBlock Text="Siap Memproses" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <TextBlock Text="0 file terpilih dalam 0 batch." Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>

                <Button Grid.Column="1" Style="{StaticResource ShadcnPrimaryButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE768;" FontSize="14"/>
                        <TextBlock Text="Mulai Proses"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

    </Grid>
</Page>
